# 交接清单 — koibito-ai（2026-09-14 收工）

> 给下一个会话用。**自包含**：读完这份 + `TODO.md` 的地图块即可直接落刀，无需重新侦察。

## 一、当前状态（实测快照）

| 项 | 值 |
|---|---|
| 仓库 | `/root/workspaces/koibito-ai-src`（远端 `sunshaoan0808/koibito-ai`） |
| `main` = `origin/main` | **`625a2ac`** ✓ 工作树干净 ✓ |
| 生产 | `koibito-ai:latest`，容器 `koibito-ai`，宿主 **:3001**（`127.0.0.1:3001`）；部署脚本 `/root/koibito-deploy/redeploy.sh` |
| 生产产物 | `index-drCX8Y5X.js`（含今天全部**已部署**内容 ✓） |
| **已落未部署** ✗ | `4cdc83e`（导出按发言者）＋ `555d025`（466 输入框）→ **说 `部署` 即走完整流程** ✓ |
| **TTS** | ✅ 正常（中文 `zh-CN-XiaoxiaoNeural` 0.718s 真 MP3）—— 详见第三节 |
| 部署记录 | `/root/koibito-deploy/DEPLOY-LOG.md`（三段，含今天的部署与 TTS 事故排查 ✓） |
| 测试基线 | `npm test` → **2685 passed** ✓；`npm run typecheck` 三套 ✓；门禁 `python3 tools/audit/check_pre_commit.py` ✓ |

## 二、今天交付的（已验证 ✓）

**已上线**：内联素材全链（解析器 → `Character.assets` → 作者 UI → VN → 经典视图 → transcript 导出 → **epub**）· Do/Say 输入模式 · 聊天标签 · 关系日记 · Tier 5 四件 · **④ 场景模板填空**（`sceneSlots`＋`createChat.slotValues`＋`NewChatDialog` 槽位输入框）· **470** VN 日志半透明 · **472** VN 空状态表情组按钮（#125）
**已落未部署**：**466** 世界开场白（`WorldCard.openingLine`＋`createChat` 前置 `role:'system'` 旁白＋`WorldsView` 作者输入框）· **导出按发言者**（`chatTranscript` 用 `rosterFrom`＋`StoredMessage.speakerId` 归属）
**修复**：**中文 TTS**（根因＝`koibito-tts` 挂了已死的 Resin 代理出口 ✗ → 改 edge-tts 直连 ✓）
**记录**：`DEPLOY-LOG.md` 三段 ✓ · skill `koibito-ai-development` 新增「TTS 链」节 ✓ · `TODO.md` 里 465/466/472 的落刀地图 ✓

## 三、TTS 链（**别再走错出口** ✗）

- 链路：`koibito-tts :5050` → `edge_tts` →（原挂 `TTS_PROXY=127.0.0.1:17890`）→ gost → **Resin :2260**（对 TTS 目标 **503** ✗）
- **本机可直连 edge-tts** ✓（实测中文 200 / 30096B / 0.72s）→ `synth()` **不要挂代理** ✓
- cf-manager 备用通路 ✓：`POST 172.17.0.1:4001/v1/audio/speech`，模型名**必须带 `@cf/` 前缀**（`@cf/deepgram/aura-2-en` ✓ 真 MP3；`@cf/myshell-ai/melotts` 是唯一中文但**当前 CF 侧 502** ✗）
- **回环勿经 `HTTP_PROXY`**（MuseAI reqwest ＋ 本次 urllib 各踩一次 ✗）→ `urllib.request.build_opener(ProxyHandler({}))` ✓
- 备份：`/opt/koibito-tts/server.py.bak-20260914-172526`（回滚＝`cp` 回 ＋ `systemctl restart koibito-tts` ✓）

## 四、剩余任务（7 项，按可落性排序）

| # | 项 | 规模 | 卡点 / 现存地图 |
|---|---|---|---|
| 1 | **`465`** 聚焦时场景暗化 | **3 文件** ✗ | 链路 `Composer.tsx→ChatWindow.tsx→VNStage.tsx`（`ChatWindow` 无聚焦态、`VNStage` 无输入框、只收 `composerHasDraft` prop ✓）；暗化＝1 行 `z-[5]` 遮罩夹在 backdrop(`z-0`) 与 chrome(`z-10`) 间 ✓ |
| 2 | **`468`** 人设↔角色契合加成 | 2-3 层 ✗ | `likes` 在 `cardSpec.ts:183` ✓ 且已进 prompt（`profile.ts:31` ✓）；**人设的兴趣字段**与**warmth 写入路径**均未定位 ✗ |
| 3 | **`466②` 已完成** ✓ | — | 见第二节 |
| 4 | **`用户脚本 / Output hook`** | 设计重 ✗ | 用户已授权"可自定边界" ✓ → **先做一次边界确认再谈代码** ✓ |
| 5 | **`组合式角色创建`** | 待核 ✗ | 先定做哪一档 ✓ |
| 6 | **群场景嫉妒「内容」** | **用户的笔** ✗ | 机制**已完成且已测** ✓（`triggers.ts:19` ＋ `intimacyStages.ts:131` ✓；见 `PLAN-jealousy-mechanics.md` ✓） |
| 7 | **路线/战役（11 天弧）** · **`Tier 6` 余项** | 大件 / 须重立项 ✗ | ⚠️ `Tier 6` 原行号映射**已失效** ✗；且其中「VN 半透明日志」与「画廊音乐室」**早已交付**（`82860bc` 09-13 ✓）→ 余项须重新清点 ✓ |

## 五、铁律与今天踩过的坑（**照做，别重踩** ✗）

1. **锚点必须从文件抄，不能凭回忆** ✗（今天因此失败 3 次：跨行锚点、错名字 `chatTranscriptTextHtml` 根本不存在、错估 `defaultBackgroundId` 有输入框）
2. **`cmd | tail/head` 会换掉退出码** ✗ → 判成败用 `cmd > /tmp/x.log 2>&1; echo $?` ✓（或裸跑 ✓）
3. **`grep -c` 为 0 时退出码是 1** ✗ → 链里写 `|| true` ✓
4. **单次 tool 写入上限**：>400 字符有截断风险、**~2000+ 必被截** ✗（字面 `[truncated]` 会落进源文件 ✗✗）→ 大段分片写 `/tmp` 再 `cat` 拼 ✓；改完必 `grep -c truncated` 验零 ✓
5. **`charactersApi.update` 必须提交完整角色对象** ✗✗（本仓 `saveCharacter.ts` 警告：会**静默丢掉**没带上的字段 ＝ 数据丢失 ✓）
6. **改完大段文档后必须查截断** ✗（本仓 `.md` 无 lint，门禁看不见 ✗）
7. **先侦察后定性** ✓✓：今天"侦察"三次挡下错误实现（epub 的 async 边界 / ④ 的 `greetingIndex` / 472 的编辑器耦合）；**硬上**的那几次全部留下红树 ✗
8. **`server.py` 类改动纪律**：备份 → 分片写 `/tmp` → `ast.parse` **先校验后写回** ✓ → restart → 端到端 curl 验证 ✓

## 六、下一步怎么开口

- `部署` → 把 `4cdc83e`＋`555d025` 推上线（锚点 → 快照 → redeploy → 新证据验收 ✓）
- `落465` / `落468` → 按上表地图机械落刀 ✓
- `用户脚本` → 先与用户做边界确认 ✓
