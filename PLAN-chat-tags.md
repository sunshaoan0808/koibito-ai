# 聊天文件夹 / 标签 方案（Tier 4 · TODO L361）

> 状态：**待你点头选 MVP 边界**，未落码。日期：2026-09-14
> 起因：我把它排在"小而独立"档，**实测后自认估低了**（见下）。按"想清楚为前置"先出方案。

## 1. 现状（取证）

| 层 | 事实 |
|---|---|
| 数据模型 | **`Chat` 没有 tags**。`types.ts:267/288` 的 `tags: string[]` 属图库/素材（服务端 `server/app.ts:253/305` 只对 gallery/image 做白名单） ✗ |
| 服务端 | chats 更新路径**无** tags/folder 字段；`server/app.ts` 里的 "folder" 全是**文件系统目录**（头像/立绘），与本项无关 ✗ |
| UI | `ChatsPanel.tsx` 里 `tag`/`folder`/`group` **零命中** ✗ |
| 规格自述 | TODO 原文：*"ROADMAP §14 open; its own authoring surface, **not a row-menu addition**"* —— 即它要一个**独立管理面**，不是往行菜单塞一项 ✗ |

**结论：全绿地（模型+服务端+UI 三层），且产品边界未定 → 不是"小项"。我上一条的"中低"排序是错的，特此纠正。**

## 2. MVP 提案（我建议的最小可用版）

**只做「标签」，先不做「文件夹」** —— 理由：标签**多对多、无需层级与拖拽**，一个字段就能表达；文件夹要多一层树 + 拖拽排序 + 空文件夹语义，成本翻倍而收益重叠。

1. **模型**：`Chat.tags?: string[]`（`types.ts` 的 `Chat` 接口），服务端 chats 更新白名单加一条（照 `app.ts:253` 的 gallery 写法：`Array.isArray` + 过滤空串 + 去重）
2. **管理面**：**不做**第二个页面。落在**聊天列表顶部一行过滤条**（点标签=过滤，有 `+` 打标签到当前聊天）—— 这就是"authoring surface"，且不依赖拖拽
3. **落库**：沿用 chats 现有 PATCH 路径（不新增表、不新增端点）

## 3. 需要你定的两件事

- **A. 边界**：就按上面的「只做标签 + 顶部过滤条」MVP 开工？还是坚持要文件夹树（成本翻倍）？
- **B. 标签的归属**：标签是**全局**一份（所有聊天共用词表）还是自由输入（谁先打谁定义）？我倾向**自由输入 + 输入时联想已有标签**（零管理负担，且天然防重复）

## 4. 验收标准（若按 MVP）

- 单测：标签归一化纯函数（trim / 去空 / 去重 / 大小写）✓
- 单测：按标签过滤聊天的纯函数 ✓
- 真机：给一个聊天打标签 → 列表顶部出现该标签 → 点击过滤出该聊天 → 刷新后仍在（落库了）✓
- tsc 三套 + 全量测试全过 ✓

## 5. 备选（如果你更想看到码而不是方案）

Tier 4 里**唯一**不需要先定产品语义的是 **RisuAI 式内联素材 `{{image::name}}`**（语法由 RisuAI 定死 ✓、可纯函数化 + 可测 ✓）。但它有一个开放项：`name` 解析到**图库条目 / 角色立绘 / 世界背景**哪一类，需要你一句定音。要我改打这个也可以。
